# resume-workflow-automation
A CLI script that takes in a bunch of skill keywords, some project bulletpoints, a jobposting, a latex resume in Jake's format, and a CV format to output an ATS optimized resume/CV specific to that job posting with an LLM.
